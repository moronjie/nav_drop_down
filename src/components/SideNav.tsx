import React from 'react'

type Props = {}

function SideNav({}: Props) {
  return (
    <div className='fixed top-0 right-0 w-full h-screen bg-black/50 backdrop-blur-2xl'>SideNav</div>
  )
}

export default SideNav